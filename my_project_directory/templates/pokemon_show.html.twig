{% extends 'base.html.twig' %}

{% block title %}Détail du Pokémon{% endblock %}

{% block body %}
    <h2>{{ pokemon.name }}</h2>
    <p>Type : {{ pokemon.type }}</p>
    <a href="{{ path('home') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}
